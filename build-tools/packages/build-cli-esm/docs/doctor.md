`flubx doctor`
==============

Checks a project for common dependency and configuration problems when using the Fluid Framework.

* [`flubx doctor`](#flubx-doctor)

## `flubx doctor`

Checks a project for common dependency and configuration problems when using the Fluid Framework.

```
USAGE
  $ flubx doctor -n <value> [-v | --quiet]

FLAGS
  -n, --name=<value>  (required) Name to print.

LOGGING FLAGS
  -v, --verbose  Enable verbose logging.
  --quiet        Disable all logging.
```

_See code: [src/commands/doctor.ts](https://github.com/microsoft/FluidFramework/blob/main/build-tools/packages/build-cli-esm/src/commands/doctor.ts)_
